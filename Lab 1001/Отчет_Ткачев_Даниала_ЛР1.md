# Лабораторная работа 1: Современные веб-технологии (React, Vite, Next.js)

**Студент:** Ткачев Даниала  
**Группа:** ПИН-б-о-24(2)

---

## Часть 1. React-приложение с Vite (To-Do List)

### 1. Описание работы

Разработано приложение "To-Do List" на стеке React + TypeScript + Vite + Tailwind CSS. Приложение позволяет добавлять, отмечать выполненными и удалять задачи, а также отображает статистику и сообщение при пустом списке.

### 2. Исходный код App.tsx

```tsx
import React, { useState } from 'react';

// Тип для задачи
interface Task {
  id: number;
  text: string;
  completed: boolean;
}

function App() {
  // Состояние для списка задач
  const [tasks, setTasks] = useState<Task[]>([
    { id: 1, text: 'Изучить React', completed: true },
    { id: 2, text: 'Написать To-Do приложение', completed: false }
  ]);

  // Состояние для новой задачи
  const [newTask, setNewTask] = useState('');

  // Функция добавления задачи
  const addTask = () => {
    if (newTask.trim() === '') return;
    const task: Task = {
      id: Date.now(),
      text: newTask,
      completed: false
    };
    setTasks([...tasks, task]);
    setNewTask('');
  };

  // Функция удаления задачи по ID
  const removeTask = (id: number) => {
    setTasks(tasks.filter(task => task.id !== id));
  };

  // Функция переключения статуса задачи
  const toggleTask = (id: number) => {
    setTasks(tasks.map(task =>
      task.id === id ? { ...task, completed: !task.completed } : task
    ));
  };

  // Подсчёт выполненных задач
  const completedCount = tasks.filter(task => task.completed).length;

  return (
    <div className="min-h-screen bg-gray-100 p-8">
      <div className="max-w-2xl mx-auto bg-white rounded-lg shadow-lg p-6">
        <h1 className="text-3xl font-bold text-center mb-6 text-gray-800">
          📝 Список задач
        </h1>
        {/* Форма добавления задачи */}
        <div className="flex gap-2 mb-6">
          <input
            type="text"
            value={newTask}
            onChange={(e) => setNewTask(e.target.value)}
            onKeyDown={(e) => e.key === 'Enter' && addTask()}
            placeholder="Введите новую задачу..."
            className="flex-grow px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <button
            onClick={addTask}
            className="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition"
          >
            Добавить
          </button>
        </div>
        {/* Список задач */}
        <div className="space-y-3">
          {tasks.map(task => (
            <div
              key={task.id}
              className="flex items-center justify-between p-3 bg-gray-50 rounded-lg border"
            >
              <div className="flex items-center gap-3">
                <input
                  type="checkbox"
                  checked={task.completed}
                  onChange={() => toggleTask(task.id)}
                  className="h-5 w-5 text-blue-600"
                />
                <span className={task.completed ? 'line-through text-gray-500' : 'text-gray-800'}>
                  {task.text}
                </span>
              </div>
              {/* Кнопка удаления */}
              <button
                onClick={() => removeTask(task.id)}
                className="px-3 py-1 text-red-500 hover:text-red-700 hover:bg-red-50 rounded"
                title="Удалить задачу"
              >
                ×
              </button>
            </div>
          ))}
          {/* Компонент для пустого списка */}
          {tasks.length === 0 && (
            <div className="text-center py-8 text-gray-500">
              <p>Список задач пуст</p>
              <p className="text-sm">Добавьте первую задачу!</p>
            </div>
          )}
        </div>
        {/* Статистика */}
        <div className="mt-6 pt-4 border-t">
          <p className="text-gray-600">
            Всего задач: {tasks.length}
          </p>
          <p className="text-gray-600">
            Выполнено: {completedCount}
          </p>
        </div>
      </div>
    </div>
  );
}

export default App;
```




### 3. Ответы на вопросы

**1. Принцип работы хука useState:**
useState — это специальная функция React, которая позволяет хранить и изменять состояние внутри компонента. Например, список задач и текст новой задачи — это состояния, которые меняются при действиях пользователя.

**2. Почему в React важно использовать неизменяемое состояние?**
Потому что React отслеживает изменения состояния по новым ссылкам на объекты. Если мутировать (изменять) старый массив, React не увидит изменений и не обновит интерфейс. Поэтому всегда создают новый массив или объект при изменениях.

**3. Какой метод массива использован для удаления задачи и почему?**
Использован метод filter. Он создаёт новый массив, исключая задачу с нужным id. Это гарантирует неизменяемость состояния и корректную работу React.

**4. Преимущества TypeScript при разработке React-приложений:**
- Помогает находить ошибки на этапе написания кода
- Улучшает автодополнение и навигацию по коду
- Делает код более понятным и поддерживаемым
- Позволяет явно описывать типы пропсов, состояния и функций

---

## Часть 2. Знакомство с Next.js (Портфолио с блогом)

### 1. Описание работы

Создано многостраничное портфолио с блогом на Next.js, TypeScript и Tailwind CSS. Реализованы страницы: главная, обо мне, блог (со статьями и динамическими маршрутами), проекты. Использованы серверный рендеринг и файловая маршрутизация.

### 2. Структура проекта

```
portfolio-site/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── about/
│   │   └── page.tsx
│   ├── blog/
│   │   ├── data.ts
│   │   ├── page.tsx
│   │   └── [slug]/
│   │       ├── page.tsx
│   │       └── not-found.tsx
│   ├── projects/
│   │   └── page.tsx
│   └── components/
│       └── ProjectCard.tsx
├── public/
├── package.json
├── tailwind.config.js
└── tsconfig.json
```

### 3. Ключевые файлы и их назначение

- **layout.tsx** — общий макет сайта (шапка, меню, футер)
- **page.tsx** — главная страница
- **about/page.tsx** — страница "Обо мне"
- **blog/data.ts** — массив статей
- **blog/page.tsx** — страница со списком статей
- **blog/[slug]/page.tsx** — динамическая страница отдельной статьи
- **blog/[slug]/not-found.tsx** — страница 404 для несуществующих статей
- **components/ProjectCard.tsx** — компонент карточки проекта
- **projects/page.tsx** — страница проектов
- **globals.css** — глобальные стили и Tailwind CSS



### 4. Ответы на вопросы

**1. Что такое SSG (Static Site Generation) и как он реализован в проекте?**
SSG — это статическая генерация страниц на этапе сборки. В проекте реализовано через функцию generateStaticParams и файловую маршрутизацию Next.js: страницы блога создаются заранее для каждого slug.

**2. Как работает файловая маршрутизация в Next.js?**
Каждый файл page.tsx внутри папки app/ становится отдельной страницей сайта. Папки с именем в квадратных скобках (например, [slug]) создают динамические маршруты.

**3. Какие преимущества даёт использование generateStaticParams?**
Позволяет заранее сгенерировать страницы для всех возможных значений параметра (например, для всех статей блога), что ускоряет загрузку и повышает SEO.

**4. В чём разница между npm run dev и npm run build?**
- npm run dev — запускает проект в режиме разработки (с горячей перезагрузкой и быстрым обновлением кода).
- npm run build — собирает оптимизированную версию проекта для продакшена (генерирует статические и серверные страницы).

---

## Итог

В ходе лабораторной работы был получен практический опыт создания современных веб-приложений на React и Next.js с использованием Vite, TypeScript и Tailwind CSS. Все основные и дополнительные требования выполнены.
